# MEMORY_ACCUMULATOR_SIMULATOR
A C-based Memory Accumulator Simulator that demonstrates OS memory management using Linked Lists, BST, Hash Tables, and Graphs. It supports Best-Fit allocation, process tracking with PIDs, cascading deallocation, memory compaction, and an interactive memory map to visualize allocation and fragmentation.
